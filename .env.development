# ─── ScholaLMS — Shared development defaults ──────────────────────────────────
# This file IS committed and used as defaults for all developers in dev mode.
# For local secrets or per-machine overrides, use .env.local (gitignored).

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=ScholaLMS
BACKEND_URL=https://lms-be-us8q.onrender.com
